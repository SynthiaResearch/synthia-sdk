# examples

- [`ci-demo/`](./ci-demo/README.md) — the minimal CI-gating setup, JS
  edition: a toy GPT-backed support agent, its `synthia.yaml`, and the
  one-time dataset bootstrap. Start here for `synthia run`.
- [`ci-demo-py/`](./ci-demo-py/README.md) — the Python twin: same agent,
  same dataset, same gates, `agent.py` entrypoint.
