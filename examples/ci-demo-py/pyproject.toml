[project]
name = "ci-demo-py"
version = "0.0.1"
description = "Toy Python billing-support agent, evaluated by Synthia in CI"
requires-python = ">=3.10"
dependencies = [
    "openai>=2",
    "synthiaresearch",
]
